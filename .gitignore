# Instructor resources — private, do not distribute to students
TRANSCRIPT.md
implementation.sh

# Local kubeconfig overrides — never commit cluster credentials
kubeconfig
*.kubeconfig

# Editor / OS noise
.DS_Store
Thumbs.db
